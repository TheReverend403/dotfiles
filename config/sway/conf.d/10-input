# vi: ft=swayconfig

input "type:keyboard" {
    xkb_layout "gb"
    xkb_options "compose:ralt,caps:super"
    xkb_numlock enabled
}

input "type:pointer" {
    accel_profile adaptive
    pointer_accel 0.0
}

input "type:touchpad" {
    dwt enabled
    tap enabled
    middle_emulation enabled
}
