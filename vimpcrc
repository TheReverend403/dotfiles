" Configuration of vimpc is done using vimpc commands
" these can include commands which require an mpd connection
"
" Copy this file to ~/.vimpcrc to use it
"
" NB: Comments are very basic and must be on their own line

" Set the default tab
set window library

" Set the windows to show at startup
set windows help,library,browse,playlist

" Ensure that the database is up to date each connect
update

map q :quitall<enter>

set nomouse
set notabbar
set albumartist
set libraryformat $D$H$H{%t}$E$R

highlight progress magentafg
highlight status magentafg
highlight current magentafg
